import React from 'react';
import FeatureCarouselWithBtn from '../../Slider/FeatureCarouselWithBtn';

export default function FeaturesSectionStyle6({ sectionTitle, data }) {
  return <FeatureCarouselWithBtn sectionTitle={sectionTitle} data={data} />;
}
